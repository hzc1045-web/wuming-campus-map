# 版本验证

版本：0.2.0，2026-09-18。

- 使用 Node.js 24.19.0 完成语法检查及 7 项内置服务器测试，全部通过。源码要求 Node.js 20 或更新版本。
- Windows start.cmd 的独立启动和端口占用回退已在 0.1.0 验证，本轮未修改启动流程。
- Edge 浏览器检查桌面 1600 × 1000、手机 390 × 844；在 /wuming-campus-map/ 子目录运行，验证相对资源路径、三种视角、湖面直接点击、新增宿舍搜索、旧名称查询、运动分类、位置服务、标签、巡游、缩放、布局图和手机地点聚焦。页面脚本错误 0、缺失资源 0。
- 人工查看鸟瞰、正上方、平面图和手机截图，与参考图核对上方宿舍、中央湖泊、湖右林地、中轴广场和右下体育区。
- 原始照片、应用截图、日志、账号配置与机器绝对路径不在源码包内。Three.js 原许可证完整保留。
- Google 连通性失败的 JSON 分支通过内置测试。实际卫星影像与地理配准仍未完成，本次发布不将其视为已验证功能。
- 文件完整性可使用根目录 MANIFEST.sha256 核对；该清单不包含自身。
