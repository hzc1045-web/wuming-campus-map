# 版本验证

版本：0.1.0，2026-09-15。

- 使用 Node.js 24.19.0 完成语法检查及 7 项内置服务器测试，全部通过。源码要求 Node.js 20 或更新版本。
- 在包含中文的独立目录中实际执行 Windows start.cmd，启动成功；默认端口被占用时选择新端口的行为通过独立验证。
- Edge 浏览器检查桌面 1440 × 1000、手机 390 × 844 的地图、搜索、视角切换和程序绘制布局图。页面脚本错误 0、缺失资源 0、初始运行外部请求 0。
- 原始照片、应用截图、日志、账号配置与机器绝对路径不在源码包内。Three.js 原许可证完整保留。
- Google 连通性失败的 JSON 分支通过内置测试。实际卫星影像与地理配准仍未完成，本次发布不将其视为已验证功能。
- 文件完整性可使用根目录 MANIFEST.sha256 核对；该清单不包含自身。